#include "Header.h"
#include <stdio.h>
void copy(){
	printf("Kopiere den Wert von y nach x\n");
			for(i=0;i<Superzahl.length2;i++)
				Superzahl.Eingabevektor1[(strlen(Buffer1)-2)-i]=0;
		
			for(i=0;i<Superzahl.length2;i++){
				Superzahl.Eingabevektor1[(strlen(Buffer2)-2)-i] = Superzahl.Eingabevektor2[(strlen(Buffer2)-2)-i];
			}
			
			for(i=0;i<(Superzahl.length2);i++)
				printf("%d",Superzahl.Eingabevektor1[i]);

		printf(" \t Zahl x\n");

			for(i=0;i<(Superzahl.length2);i++)
				printf("%d",Superzahl.Eingabevektor2[i]);

		printf(" \t Zahl y\n");
		printf("----------------------------------------------------\n");
		}